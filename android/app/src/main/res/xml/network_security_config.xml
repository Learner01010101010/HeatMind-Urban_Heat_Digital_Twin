<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        HTTPS everywhere by default. Cleartext is permitted only to addresses that
        cannot leave the developer's own machine or LAN, so a debug build can talk
        to `npm run dev` without weakening anything that ships.

        10.0.2.2 is the emulator's alias for the host. The private ranges cover a
        laptop on the same Wi-Fi as a physical device.
    -->
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
    </domain-config>
</network-security-config>
